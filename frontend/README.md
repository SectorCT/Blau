# Blau Frontend

Simple React + TypeScript frontend for the backend API domains:

- Authentication
- Studies
- Measurements
- Filters

## Run locally

```bash
npm install
cp .env.example .env
npm run dev
```

The default API endpoint is `http://localhost:8000` and requests are sent to `/api/*`.
